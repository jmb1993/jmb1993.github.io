var player1 = function(){

  player1 = prompt("Enter your name");

}
var player2 = function(){
  player2 = prompt("Enter your name");
}
var game = function(){
  players: (player1, player2)
  active: true
  var move = function(){ $("div").click(function(){
  alert("clicked");
  });
  };

var checkForWin = function(){
  if [ array of selected adjacent divs= black or red ] alert("player1 wins"), or
  alert("player2 wins")
  else if [ 2 arrays of selected adjacent divs = black && red] alert("tie game!")
  else {keep playing until 1 of above conditions is met}


}
}
/*
var play = function{
$("div").on("click", function(){
  var currentPlayer = prompt("enter 1 or 2");
  if( currentPlayer == "1" && $(this != clickedCell[])){
    $(this).attr("id", "player1");
    board.clickedCell[].push($(this));
  }
    else if( currentPlayer == "2" && $(this != clickedCell[])) {
          $(this).attr("id", "player2");
board.clickedCell[].push($(this));}
    } else { alert("invalid response, pick a correct number and an empty space!");
           }

      );

}













/*
if $("div" #clicked) alert("this space has already been chosen!")
var slideDown = function(){
  if (("div-1" != #clicked) slide())
  else if (bottom of grid) break;
  else



};

/*var board = []
42 cells (6 x 7)
var buildBoard = function(){
var column = $("column")
for( var i = 0; i <= 7; i++){
board.push(column)
var row = $("row")
for(var j = 0; j <=6; j++){
board.push(row)
}
}
}

var setName = function(player1,player2){
}
markSpace = if space open mark it red or black depending on which players turn it is
if full alert("invalid choice, pick again"), if cell below choice isnt full alert("must be placed on top of full space or on bottom")

$("div").on("click", function(e){ prompt user to markSpace
return markSpace()
})

checkForWin = function(){
if (4 cells adjacent(vertical, horizontal, diagonal) cells are full with the same color choice)
which ever player is that color wins
}
else if 2 sets of adjacent cells are full (both colors){
tie game!
}
else {
game continues until one of these outcomes occurs
}

*/
